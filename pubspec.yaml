name: flutterapp
description: dongdong_recorder

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  path_provider: ^1.5.0
  flutter_plugin_record: ^0.1.2
  provider: ^4.1.2
  event_bus: ^1.1.0
  dio: ^3.0.8
  crypto: ^2.1.2
  convert: ^2.1.1
  permission_handler: ^5.0.0+hotfix.6
  flutter_echarts: ^1.3.5
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - asset/
    - asset/edit/
    - asset/flag/
    - asset/home_recording/
    - asset/palying/
    - asset/setting/
    - asset/sheared/
    - asset/toolbar/
    - asset/sheared/
